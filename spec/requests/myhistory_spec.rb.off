require 'rails_helper'

RSpec.describe "Myhistories", type: :request do
  describe "GET /index" do
    it "returns http success" do
      get "/myhistory/index"
      expect(response).to have_http_status(:success)
    end
  end

end
